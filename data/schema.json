{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "companions": [
    {
      "description": "This file. Every repository, one lean record each, plus the similarity edges.",
      "path": "data/index.json",
      "url": "https://moses-y.github.io/data/index.json"
    },
    {
      "description": "Repositories grouped into clusters of near-duplicate work, with the grade of each member and which one is the keeper.",
      "path": "data/clusters.json",
      "url": "https://moses-y.github.io/data/clusters.json"
    },
    {
      "description": "The same clusters as prose: which group crosses a domain, which member is the keeper, and how much of each group has never been audited. For a reader who wants the argument rather than the records.",
      "path": "data/clusters.md",
      "url": "https://moses-y.github.io/data/clusters.md"
    },
    {
      "description": "One repository's neighbourhood: nearest semantic kin and repositories sharing its dependency stack. One small fetch per hop, so walking the graph does not require parsing this file.",
      "path": "data/kin/<id>.json",
      "url": "https://moses-y.github.io/data/kin/<id>.json"
    },
    {
      "description": "Deterministic grade per audited repository across eight weighted categories. No model is involved.",
      "path": "data/grades.json",
      "url": "https://moses-y.github.io/data/grades.json"
    },
    {
      "description": "The same grades with the explanation removed: id -> [score, letter, partial]. About 37 KB against 2.9 MB, for a consumer that wants one number per repository rather than the reasoning. An id absent from it has not been audited, which is not a grade.",
      "path": "data/grade-map.json",
      "url": "https://moses-y.github.io/data/grade-map.json"
    },
    {
      "description": "Raw audit findings by severity, the input to grades.json.",
      "path": "data/hygiene.json",
      "url": "https://moses-y.github.io/data/hygiene.json"
    },
    {
      "description": "Declared dependencies per repository by ecosystem.",
      "path": "data/deps.json",
      "url": "https://moses-y.github.io/data/deps.json"
    },
    {
      "description": "Inverted index: token -> positions in index.json `repos`.",
      "path": "data/search.json",
      "url": "https://moses-y.github.io/data/search.json"
    }
  ],
  "description": "The read side of a deterministic analysis pipeline over every repository in the estate. Record keys are single letters to keep the file small; this document is the key. Every field in a record is EXTRACTED - measured from the repository tree, its history or its manifests, with no model involved - with one exception: `u` is a projection of a neural embedding, and the `links` array derived from it is therefore INFERRED. See /data/relations.json for the provenance of every edge type.",
  "licence": "Facts about public repositories. Reuse freely; attribution appreciated.",
  "record": {
    "L": {
      "description": "File count by language, present only on the records where `l` is null. EXTRACTED from the file census. Both graph pages derive a repository's language from `l` and fall back to the largest entry here, so carrying it for these records alone is what lets them read this file instead of the 7 MB feed.",
      "type": "object|absent"
    },
    "a": {
      "description": "1 when a written briefing exists at /blog/<n>.html.",
      "type": "0|1"
    },
    "c": {
      "description": "Five capability bits as characters, in order: hasTests, hasCI, hasDocker, hasLicense, committedSecrets. \"11110\" means tested, CI, Docker, licensed, no secrets found. The fifth bit is the only one where 1 is bad.",
      "type": "string"
    },
    "d": {
      "description": "Description, truncated to 180 characters.",
      "type": "string"
    },
    "f": {
      "description": "Total files in the tree.",
      "type": "integer"
    },
    "g": {
      "description": "Domain. One of: AI & Data, Web & Interfaces, Systems & Infra, Mobile, Knowledge & Content, Agent Skills & Plugins, Other. Recomputed at index time from the language and the file census, so it is a function of the tree and not a stored label.",
      "type": "string"
    },
    "i": {
      "description": "GitHub repository id. Stable across renames and the join key for every other file in data/.",
      "type": "integer"
    },
    "k": {
      "description": "Repository kind, e.g. \"Web app\", \"Library\", \"CLI\". Coarser than the domain and used for grading profiles.",
      "type": "string|null"
    },
    "l": {
      "description": "Primary code language, derived from the file census rather than the GitHub API, which returns null on most forks. Null means the tree holds no code language - prose and config repositories are the usual case.",
      "type": "string|null"
    },
    "m": {
      "description": "Card image URL.",
      "type": "string|absent"
    },
    "n": {
      "description": "Repository slug. Also the article path: /blog/<n>.html.",
      "type": "string"
    },
    "p": {
      "description": "Upstream parent for a fork: {n: full name, u: URL, s: stars}.",
      "type": "object|absent"
    },
    "r": {
      "description": "Estimated read time of the briefing, in minutes.",
      "type": "integer|absent"
    },
    "s": {
      "description": "Stars on the upstream repository.",
      "type": "integer"
    },
    "t": {
      "description": "Display title. Falls back to the slug when no title was set.",
      "type": "string"
    },
    "u": {
      "description": "UMAP projection of a neural embedding (see relations.json for the model) of the description, topics, languages, frameworks and generated summary, 4 decimal places. INFERRED, not measured: the only field in a record that a model produced. The same space `links` is drawn from, so two repositories close in `u` are probably close in meaning.",
      "type": "number[3]|absent"
    },
    "v": {
      "description": "Hygiene findings by severity: [critical, high, medium, low]. The scalar 0 means audited and clean. Absent means not audited - which is not the same as clean, and the two must not be collapsed.",
      "type": "integer[4]|0|absent"
    },
    "x": {
      "description": "Count of structural issues found by the architecture pass.",
      "type": "integer"
    },
    "y": {
      "description": "1 when this is an original repository, 0 when it is a fork.",
      "type": "0|1"
    },
    "z": {
      "description": "Last update, already formatted for display.",
      "type": "string|absent"
    }
  },
  "source": "https://moses-y.github.io/data/index.json",
  "title": "moses-y.github.io repository index",
  "top": {
    "generated": {
      "description": "ISO timestamp of the feed this index was built from.",
      "type": "string"
    },
    "links": {
      "description": "Semantic similarity edges as [sourceId, targetId, similarity]. Undirected despite the ordering, cosine similarity in the embedding space, 3 decimal places. Both ids are `i` values in `repos`. INFERRED - carries no evidence beyond the score. The EXTRACTED counterpart, shared declared dependencies with the packages named, is in /data/kin/<id>.json.",
      "type": "array"
    },
    "repos": {
      "description": "The records. See `record`.",
      "type": "array"
    },
    "taxonomy": {
      "description": "Counts by domain, language, kind and capability. `domains` is recounted from `repos` at build time; the rest is carried from the feed.",
      "type": "object"
    },
    "total": {
      "description": "Number of records in `repos`.",
      "type": "integer"
    },
    "totals": {
      "description": "Estate-wide counts: files, findings, links, withArticle.",
      "type": "object"
    }
  }
}
